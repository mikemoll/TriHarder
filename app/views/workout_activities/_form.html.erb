<%= form_for(@workout_activity) do |f| %>
  <% if @workout_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workout_activity.errors.count, "error") %> prohibited this workout_activity from being saved:</h2>

      <ul>
      <% @workout_activity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Type %><br />
    <%= f.text_field :Type %>
  </div>
  <div class="field">
    <%= f.label :Description %><br />
    <%= f.text_area :Description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
